import { AppointmentOrderPipe } from './appointment-order.pipe';

describe('AppointmentOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
